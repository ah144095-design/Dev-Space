# DevSpace Project

## Overview
This repository contains the **DevSpace** application, a Windows desktop application built with C# and WPF. It includes the core application code, UI views, models, data access layers, and helper utilities.

## Directory Structure
```
DevSpace/
├─ .vs/                     # Visual Studio configuration files
├─ bin/                     # Compiled binaries (output)
├─ obj/                     # Intermediate build files
├─ App.xaml                 # Application resources
├─ App.xaml.cs              # Application entry point
├─ AssemblyInfo.cs          # Assembly metadata
├─ DevSpace.csproj          # Project file
├─ Converters/              # Value converters for XAML bindings
├─ Database/                # Database schema and seed files
├─ Dialogs/                 # Dialog window definitions
├─ Helpers/                 # Utility classes and extensions
├─ Models/                  # Data models representing domain entities
├─ Repositories/            # Data access repository implementations
├─ Themes/                  # Application theming and styles
├─ Views/                   # XAML UI views (windows, user controls)
└─ README.md                # **This** file – project documentation
```

## Creating a Report
To add a report about the project (e.g., design documents, analysis, or usage guides), follow these steps:
1. **Create a `Reports` folder** at the root of the repository if it does not already exist.
2. Inside `Reports/`, add a Markdown (`.md`) or PDF file for your report, for example `Reports/DesignOverview.md`.
3. Commit the new files to version control (e.g., Git) so they are tracked alongside the source code.

### Example Commands (Git)
```bash
# From the repository root
mkdir Reports
cd Reports
# Create a new Markdown report
echo "# Design Overview\n\n..." > DesignOverview.md
# Add and commit
git add Reports/DesignOverview.md
git commit -m "Add design overview report"
```

## Building & Running the Application
1. Open the solution in Visual Studio 2022 or later.
2. Restore NuGet packages if prompted.
3. Build the solution (`Ctrl+Shift+B`).
4. Run the application (`F5`).

---
*Generated by Antigravity AI assistant.*
